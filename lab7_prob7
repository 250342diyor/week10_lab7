#include <iostream>
using namespace std;
int sumofnum(int n) {
    int sum;
    if (n == 0) {
        return 0;
    }
    sum = n % 10;
    return sum + sumofnum(n / 10);
}
int main() {
    int n;
    cout << "Enter a number: ";
    cin >> n;
    cout<<"Sum of digits: " << sumofnum(n) << endl;
}
